// Legacy compatibility placeholder.
// Data is now persisted via MongoDB models under server/src/models.
export const legacyStore = Object.freeze({
  users: [],
  posts: [],
  gigs: []
});

export default legacyStore;
